{
  "name": "md-links",
  "version": "0.1.0",
  "engines": {
    "node": ">=16.x"
  },
  "main": "index.js",
  "scripts": {
    "eslint": "eslint --ext .js src/ test/",
    "test": "jest --coverage"
  },
  "dependencies": {
    "markdown-it": "^13.0.1"
  },
  "devDependencies": {
    "jest": "^29.2.0"
  }
}
