const path = require('path');

const directoryPath = argv[2];
console.log(directoryPath);
const resolverpath = path.resolve(directoryPath);
console.log(resolverpath);


module.exports = {
    absolute
}